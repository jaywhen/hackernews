export type StoryData = {
  id: number,
  by: string,
  title: string,
  url: string,
  hostname: string,
  time: string,
  score: number,
}

export type Nav = {
  id: number,
  link: string,
  name: string,
}